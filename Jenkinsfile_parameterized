#!groovy
//noinspection GroovyUnusedAssignment
@Library("Infrastructure") _

//noinspection GroovyAssignabilityCheck
properties([
		parameters([
			choice(name: 'ENVIRONMENT', choices: 'sandbox\nnonprod\nprod', description: 'Environment where code should be build and deployed')
		])
])

List<LinkedHashMap<String, Object>> secrets = [

]

static LinkedHashMap<String, Object> secret(String secretName, String envVar) {
  [ $class: 'AzureKeyVaultSecret',
    secretType: 'Secret',
    name: secretName,
    version: '',
    envVariable: envVar
  ]
}

withParameterizedPipeline('nodejs', 'div', 'frontend', params.ENVIRONMENT, 'sandbox') {
  before('build') {
    sh 'sudo pip install --upgrade pip'
    sh 'sudo pip uninstall docker -y'
    sh 'sudo pip uninstall docker-py --no-cache-dir'
    sh 'sudo pip install docker==3.1.4'
    sh 'sudo pip list | grep docker'
    sh 'yarn test:smoke'
  }
  
  after('build') {
    sh 'yarn setup'
  }

  loadVaultSecrets(secrets)
}
